---
description: >
  Shared memory index for all agents: one line per memory (one fact or piece of
  feedback per file in this folder). Takes the place of the agent's native memory,
  which would otherwise exist only locally and per agent. That way memories stay in
  the repo and are available to every agent. Likewise for memories that matter to the user.
type: memory
edit: shared
---

# Memory

<!-- Instead of living in a single agent's native memory (only local on the machine),
     memories are stored here in the repo and travel with the project.

     One entry per memory, one line: - [Title](file.md) - short hook. -->
